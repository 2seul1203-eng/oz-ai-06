// 논리연산자(Logical Operator)
// 여러 개의 참/거짓 조건을 조합해서 판별

// and 조건: &&
// or 조건: ||

let age = 20;
let hasId = true;

console.log(age >= 19 && hasId); // ture
console.log(age < 19 || hasId); // ture
console.log(!hasId); // not ture